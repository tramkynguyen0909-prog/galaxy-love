<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<title>Galaxy Love</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
html, body {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    background: black;
    overflow: hidden;
    font-family: Arial, sans-serif;
    color: white;
}

/* NỀN GALAXY */
body::before {
    content: "";
    position: fixed;
    inset: 0;
    background:
        radial-gradient(circle at center, rgba(255,255,255,0.15), transparent 60%),
        url("https://i.imgur.com/8uucQ2B.png") center/cover no-repeat;
    animation: spin 120s linear infinite;
    z-index: -2;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

/* SAO BAY */
.star {
    position: absolute;
    width: 2px;
    height: 2px;
    background: white;
    animation: move 10s linear infinite;
}

@keyframes move {
    from { transform: translateY(0); opacity: 1; }
    to { transform: translateY(100vh); opacity: 0; }
}

/* ẢNH */
.gallery img {
    position: absolute;
    width: 140px;
    border-radius: 12px;
    cursor: pointer;
    transition: transform 0.4s;
}

.gallery img:hover {
    transform: scale(1.2);
    z-index: 10;
}

/* CHỮ */
#lyrics {
    position: fixed;
    bottom: 20%;
    width: 100%;
    text-align: center;
    font-size: 22px;
    opacity: 0;
    transition: opacity 1s;
}

/* KẾT */
#end {
    position: fixed;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 40px;
    background: black;
    opacity: 0;
    pointer-events: none;
    transition: opacity 2s;
}
</style>
</head>

<body>

<!-- NHẠC -->
<audio autoplay loop>
    <source src="https://files.catbox.moe/2xk7tb.mp3" type="audio/mpeg">
</audio>

<!-- ẢNH -->
<div class="gallery">
    <img src="https://i.imgur.com/1.jpg" style="top:10%;left:10%">
    <img src="https://i.imgur.com/2.jpg" style="top:30%;left:70%">
    <img src="https://i.imgur.com/3.jpg" style="top:60%;left:20%">
    <img src="https://i.imgur.com/4.jpg" style="top:65%;left:65%">
</div>

<!-- CHỮ -->
<div id="lyrics">💖 Tình cờ yêu em… 💖</div>

<!-- KẾT -->
<div id="end">TÔI YÊU BÀ ❤️</div>

<script>
/* SAO */
for (let i = 0; i < 80; i++) {
    const s = document.createElement("div");
    s.className = "star";
    s.style.left = Math.random() * 100 + "vw";
    s.style.top = Math.random() * 100 + "vh";
    s.style.animationDuration = 5 + Math.random() * 10 + "s";
    document.body.appendChild(s);
}

/* CHỮ HIỆN */
setTimeout(() => {
    document.getElementById("lyrics").style.opacity = 1;
}, 2000);

/* KẾT MÀN */
setTimeout(() => {
    document.getElementById("lyrics").style.opacity = 0;
    document.getElementById("end").style.opacity = 1;
}, 20000);
</script>

</body>
</html>
